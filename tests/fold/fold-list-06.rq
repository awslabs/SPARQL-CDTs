PREFIX cdt:    <http://example.org/cdt/>

ASK {
	{
		SELECT (FOLD(?v) AS ?list)
		WHERE {
			VALUES ?v { 1 UNDEF 2 }
		}
	}
	FILTER(cdt:get(?list, 1) = 1)
	FILTER(cdt:get(?list, 3) = 2)
	BIND(cdt:get(?list, 2) AS ?null)
	FILTER(!BOUND(?null))
}