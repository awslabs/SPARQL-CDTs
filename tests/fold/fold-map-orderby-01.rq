PREFIX cdt:    <http://example.org/cdt/>

ASK {
	{
		SELECT (FOLD(?k, ?v ORDER BY ?sort) AS ?map)
		WHERE {
			VALUES (?k ?v ?sort) {
				(1 100  "irrelevant")
				(2 201  1)
				(2 203  3)
				(2 202  2)
			}
		}
	}
	FILTER( ?map = "{1:100, 2:203}"^^cdt:Map )
}
