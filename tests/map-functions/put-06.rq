PREFIX cdt:    <http://example.org/cdt/>

ASK {
	BIND( "{1:null, 2:'two'}"^^cdt:Map AS ?mapIn )
	BIND( cdt:put(?mapIn, 1, "one") AS ?mapOut )

	FILTER( BOUND(?mapOut) )    # check that there was no error
	FILTER( ?mapOut = "{1:'one', 2:'two'}"^^cdt:Map )
}
